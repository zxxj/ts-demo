

// 规定数据中的内容只能为number类型
const ageList: number[] = [30, 18, 22]
ageList.push(19)

// 元素类型,可以为数组指定数据类型,如下: userInfoList中的类型只能为string, number, boolean
const userInfoList: [string, number, boolean] = ['xinxin', 18, true]
userInfoList.push(20)

